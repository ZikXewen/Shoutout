import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  progress: {
    margin: "30px",
  },
  posts: {
    display: "flex",
    flexDirection: "column",
    margin: "20px 0",
  },
}));
